export enum Roles {
  Admin = 'admin',
  Moderator = 'moderator',
  User = 'user'
}

export enum Routes {
  Login = '/login',
  Register = '/register',
  Dashboard = '/dashboard',
  Users = '/users',
  UsersCreate = '/users/create',
  Posts = '/posts',
  Error = '/error',
  NotFound = '*'
}
