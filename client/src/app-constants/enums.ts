export enum Roles {
  Admin = 'admin',
  Moderator = 'moderator',
  User = 'user'
}

export enum Routes {
  Login = '/login',
  Register = '/register',
  Dashboard = '/dashboard',
  Error = '/error',
  NotFound = '*'
}
